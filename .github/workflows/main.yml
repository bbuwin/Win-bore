name: Bore RDP - Public Repo Secure

on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 360
    
    steps:
    - name: Download Bore
      run: |
        Invoke-WebRequest -Uri "https://github.com/ekzhang/bore/releases/download/v0.5.0/bore-v0.5.0-x86_64-pc-windows-msvc.zip" -OutFile "bore.zip"
        Expand-Archive -Path "bore.zip" -DestinationPath "."
        
    - name: Enable RDP
      run: |
        # Enable RDP
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        netsh advfirewall firewall add rule name="RDP" dir=in protocol=tcp localport=3389 action=allow
        
        # Create user from Secrets
        net user ${{ secrets.RDP_USERNAME }} ${{ secrets.RDP_PASSWORD }} /add
        net localgroup administrators ${{ secrets.RDP_USERNAME }} /add
        
        Start-Service TermService -ErrorAction SilentlyContinue
        
        # Verify RDP is actually listening
        Write-Host "Verifying RDP is listening..."
        netstat -an | findstr ":3389"
        
    - name: Start Bore Tunnel
      run: |
        Write-Host ""
        Write-Host "========================================="
        Write-Host "✅ BORE RDP READY"
        Write-Host "========================================="
        Write-Host "Username: ${{ secrets.RDP_USERNAME }}"
        Write-Host "Password: [HIDDEN IN SECRETS]"
        Write-Host ""
        Write-Host "Looking for Bore address..."
        Write-Host "========================================="
        Write-Host ""
        
        # Run Bore in foreground (this keeps it alive)
        .\bore.exe local 3389 --to bore.pub
        
        # Keep alive (this line only runs if Bore exits)
        while ($true) { 
            Write-Host "RDP session active..."
            Start-Sleep -Seconds 300 
        }
